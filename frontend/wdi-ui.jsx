import React from "react";
import ReactDOM from "react-dom";

document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  ReactDOM.render(
    <h2>Welcome to the Workforce Data Initiative's Open Skills API!</h2>,
    root
  );
});
